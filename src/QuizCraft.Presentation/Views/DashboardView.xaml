<UserControl x:Class="QuizCraft.Presentation.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:QuizCraft.Presentation.Converters">
    <UserControl.Resources>
        <converters:DoubleToPercentageConverter x:Key="PctConverter" />
        <converters:PercentageToColorConverter x:Key="PctColorConverter" />
        <converters:PercentageToHeightConverter x:Key="PctToHeightConverter" />
        <converters:PercentageToBarWidthConverter x:Key="PctToBarWidthConverter" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,24">
            <TextBlock Text="Dashboard" Style="{StaticResource PageTitle}" />

            <!-- Stats Cards Row 1 -->
            <UniformGrid Columns="4" Margin="0,0,0,16">
                <Border Style="{StaticResource CardBorder}" Margin="0,0,8,8">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalQuestions}" Style="{StaticResource StatValue}" />
                        <TextBlock Text="Questões Totais" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="4,0,4,8">
                    <StackPanel>
                        <TextBlock Text="{Binding QuestionsStudied}" Style="{StaticResource StatValue}" />
                        <TextBlock Text="Questões Estudadas" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="4,0,4,8">
                    <StackPanel>
                        <TextBlock Text="{Binding AccuracyRate7Days, Converter={StaticResource PctConverter}}"
                                   Style="{StaticResource StatValue}"
                                   Foreground="{Binding AccuracyRate7Days, Converter={StaticResource PctColorConverter}}" />
                        <TextBlock Text="Acertos (7 dias)" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="8,0,0,8">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentStreak}" Style="{StaticResource StatValue}"
                                   Foreground="#FF9800" />
                        <TextBlock Text="Dias Seguidos" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Stats Cards Row 2 -->
            <UniformGrid Columns="4" Margin="0,0,0,16">
                <Border Style="{StaticResource CardBorder}" Margin="0,0,8,8">
                    <StackPanel>
                        <TextBlock Text="{Binding AccuracyRate30Days, Converter={StaticResource PctConverter}}"
                                   Style="{StaticResource StatValue}"
                                   Foreground="{Binding AccuracyRate30Days, Converter={StaticResource PctColorConverter}}" />
                        <TextBlock Text="Acertos (30 dias)" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="4,0,4,8">
                    <StackPanel>
                        <TextBlock Text="{Binding AverageTimeSeconds, StringFormat={}{0:F0}s}"
                                   Style="{StaticResource StatValue}" />
                        <TextBlock Text="Tempo Médio/Questão" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="4,0,4,8">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalSessions}" Style="{StaticResource StatValue}" />
                        <TextBlock Text="Sessões Realizadas" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="8,0,0,8">
                    <StackPanel>
                        <TextBlock Text="{Binding DueForReview}" Style="{StaticResource StatValue}"
                                   Foreground="#F44336" />
                        <TextBlock Text="Pendentes p/ Revisão" Style="{StaticResource StatLabel}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Charts -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Daily Accuracy Chart -->
                <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Taxa de Acerto ao Longo do Tempo" Style="{StaticResource SectionTitle}" />
                        <ItemsControl ItemsSource="{Binding DailyAccuracyBars}" Height="200">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="2,0" VerticalAlignment="Bottom">
                                        <TextBlock Text="{Binding Value, StringFormat={}{0:F0}%}" FontSize="9"
                                                   HorizontalAlignment="Center" Opacity="0.7" />
                                        <Border Background="#2196F3" CornerRadius="2,2,0,0"
                                                HorizontalAlignment="Stretch" MinHeight="4"
                                                Height="{Binding Value, Converter={StaticResource PctToHeightConverter}}" />
                                        <TextBlock Text="{Binding Label}" FontSize="9"
                                                   HorizontalAlignment="Center" Opacity="0.6" Margin="0,4,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Topic Performance Chart -->
                <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Desempenho por Tópico" Style="{StaticResource SectionTitle}" />
                        <ItemsControl ItemsSource="{Binding TopicBars}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="40" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Label}" FontSize="11" VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />
                                        <Border Grid.Column="1" Background="#E0E0E0" CornerRadius="3" Height="14">
                                            <Border Background="#4CAF50" CornerRadius="3"
                                                    HorizontalAlignment="Left"
                                                    Width="{Binding Value, Converter={StaticResource PctToBarWidthConverter}}" />
                                        </Border>
                                        <TextBlock Grid.Column="2" Text="{Binding Value, StringFormat={}{0:F0}%}"
                                                   FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Weak Topics -->
            <Border Style="{StaticResource CardBorder}" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="Tópicos Mais Fracos" Style="{StaticResource SectionTitle}" />
                    <ItemsControl ItemsSource="{Binding WeakTopics}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding TopicName}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding SubjectName}" FontSize="11" Opacity="0.6" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding TotalAttempts, StringFormat={}{0} tentativas}"
                                               VerticalAlignment="Center" Margin="16,0" Opacity="0.6" />
                                    <ProgressBar Grid.Column="2" Value="{Binding AccuracyRate}" Maximum="100"
                                                 Height="8" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
